<article class="card">
  <h3><a href="{{ project.url }}" rel="me">{{ project.name }}</a></h3>
  <span class="badge badge--{{ project.status }}">{{ project.status }}</span>
  <p>{{ project.description }}</p>
</article>
