<footer>
  <nav aria-label="Secondary">
    <ul>
      {%- for item in navigation.secondary %}
      <li><a href="{{ item.url }}"{% if item.external %} rel="external" target="_blank"{% endif %}>{{ item.label }}</a></li>
      {%- endfor %}
    </ul>
  </nav>
  {%- set lastModified = page.inputPath | gitLastModified %}
  {%- if lastModified %}
  <p class="footer-meta">
    <time class="dt-updated" datetime="{{ lastModified }}">Last updated {{ lastModified | readableDate }}</time>
  </p>
  {%- endif %}
  <p class="kbd-hint">Press <kbd>?</kbd> for shortcuts &middot; <a href="/keyboard/">Keyboard reference</a></p>
  <p>&copy; {{ site.author }}</p>
</footer>
